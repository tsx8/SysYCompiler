/* Description:
 *   Converted from `codes/2023fa/exam/E3/B.c`.
 *   Reads 32 binary digits (0/1), most-significant bit first, and prints the
 *   resulting integer in decimal.
 */

int main() {
    int n;
    int i;
    int x;
    n = 0;
    for (i = 0; i < 32; i = i + 1) {
        x = getint();
        n = n * 2 + x;
    }
    printf("%d", n);
    return 0;
}
