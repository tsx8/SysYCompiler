/* Description:
 *   Converted from C++: struct + references (refactored away for Strict SysY).
 *   Observes final values via printf.
 */

const int MAXN = 1111;
int p_x[MAXN];

int main() {
    int y;
    int x;

    p_x[1] = 1;
    y = 1;
    x = y;

    printf("%d\n", p_x[1]);
    printf("%d\n", x);
    return 0;
}
